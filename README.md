Sources for the plugin `plugin.appodeal.Unity`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.appodeal.Unity" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
